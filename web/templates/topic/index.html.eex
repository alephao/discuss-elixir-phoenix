<h3>Topics</h3>
<%= link "New topic", to: topic_path(@conn, :new), class: "btn btn-primary", role: "button" %>
<br/>
<br/>
<table class="table">
  <tbody>
    <%= for topic <- @topics do %>
      <tr>
        <td><%= topic.title %></td>
        <td class="text-right">
          <%= link "Edit", to: topic_path(@conn, :edit, topic) %>
          &nbsp;
          <%= link "Delete", to: topic_path(@conn, :delete, topic), method: :delete %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>